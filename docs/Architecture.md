# Areakart Architecture

Frontend (Next.js)

↓

Backend (FastAPI)

↓

PostgreSQL Database

Users interact with the frontend.

The frontend communicates with the backend.

The backend fetches data from the database.

The results are returned to the frontend.